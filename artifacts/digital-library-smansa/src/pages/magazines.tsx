import { ArrowUpRight, BookOpen, CalendarDays, FileText } from 'lucide-react';
import { useListMagazines } from '@workspace/api-client-react';
import { CardSkeleton, EmptyState, QueryError } from '@/components/feedback';

export default function Magazines() {
  const query = useListMagazines();
  const magazines = query.data ?? [];
  return <div className="mx-auto max-w-7xl px-5 py-12 sm:px-8 lg:px-10 lg:py-16"><div className="grid gap-8 border-b border-border pb-12 md:grid-cols-[1.1fr_.9fr] md:items-end"><div><p className="font-mono-display text-[10px] font-bold uppercase tracking-[.2em] text-primary">Ruang baca digital</p><h1 className="mt-3 max-w-2xl font-display text-4xl font-bold leading-tight sm:text-6xl">Cerita sekolah, dalam sekali klik.</h1></div><p className="max-w-md text-sm leading-7 text-muted-foreground">Kumpulan majalah dan terbitan digital warga SMAN 1 Bukittinggi. Baca kapan saja, dari halaman pertama sampai terakhir.</p></div><div className="mt-12">{query.isError ? <QueryError onRetry={() => query.refetch()} /> : query.isLoading ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="skeleton h-72 rounded-2xl" />)}</div> : magazines.length ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{magazines.map((magazine) => <article key={magazine.id} className="group overflow-hidden rounded-2xl border border-border bg-card p-3 transition hover-lift" data-testid={`card-magazine-${magazine.id}`}><div className="relative aspect-[16/10] overflow-hidden rounded-xl bg-secondary"><img src={magazine.coverUrl} alt={`Sampul ${magazine.title}`} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /><span className="absolute left-3 top-3 rounded-full bg-card/90 px-3 py-1 font-mono-display text-[10px] font-bold uppercase tracking-widest text-primary backdrop-blur">{magazine.edition}</span></div><div className="p-3 pt-4"><div className="flex items-center gap-2 text-xs text-muted-foreground"><CalendarDays size={13} className="text-primary" /> {magazine.year}</div><h2 className="mt-2 font-display text-2xl font-bold leading-tight">{magazine.title}</h2><p className="mt-3 line-clamp-2 text-sm leading-6 text-muted-foreground">{magazine.description}</p><a href={magazine.readUrl || '#'} target="_blank" rel="noreferrer" aria-disabled={!magazine.readUrl} onClick={(event) => { if (!magazine.readUrl) event.preventDefault(); }} data-testid={`link-read-magazine-${magazine.id}`} className={`mt-5 inline-flex items-center gap-2 text-sm font-bold ${magazine.readUrl ? 'text-primary hover:gap-3' : 'cursor-not-allowed text-muted-foreground'}`}>{magazine.readUrl ? 'Baca edisi' : 'Belum tersedia'} {magazine.readUrl ? <ArrowUpRight size={15} /> : <FileText size={15} />}</a></div></article>)}</div> : <EmptyState title="Belum ada edisi digital" description="Majalah sekolah akan hadir di ruang baca ini." />}</div><div className="mt-14 flex items-center gap-4 rounded-2xl border border-border bg-secondary/55 p-5 sm:p-6"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-primary text-primary-foreground"><BookOpen size={21} /></span><p className="text-sm leading-6 text-muted-foreground">Ingin mengusulkan tulisan untuk edisi berikutnya? <span className="font-semibold text-foreground">Hubungi tim perpustakaan.</span></p></div></div>;
}